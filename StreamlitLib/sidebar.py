import streamlit as st
import time

# number = st.sidebar.slider("Choose a number", min_value=0, max_value=100)
# st.metric("Selected number", number)

# with st.sidebar:
#     with st.echo():
#         st.write("This code will be printed to the sidebar.")

#     with st.spinner("Loading..."): spinner = c'est pr attendre, et succes c'est le resulat (text qui s'affiche)
#         time.sleep(5)
#     st.success("Done!")